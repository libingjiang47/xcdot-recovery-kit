# Claims

Claim records, signatures, beneficiary mapping, and Merkle trees are intentionally out of scope for v0.1. A future claim dataset must be derived from an approved recovery policy, not directly from `holders.ndjson`.
